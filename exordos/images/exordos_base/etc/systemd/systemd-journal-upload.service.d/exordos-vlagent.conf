# Ensure journal-upload only starts after the local vlagent is ready,
# so it can immediately connect to the /insert/journald HTTP endpoint.
[Unit]
After=exordos-vlagent.service
Wants=exordos-vlagent.service
